import React from "react";
import { Grid, Button } from "../elements";

const PostButton = (props) => {
  return(
    <React.Fragment>
      <Grid height="40px" margin="4px 0 0 0">
        <Button width="30%">아이콘</Button>
      </Grid>
      
    </React.Fragment>
  );
}

export default PostButton;
