import React from "react";
import { history } from "../redux/configStore";

const SignUp = (props) => {
  return(
    <React.Fragment>
      회원가입페이지
    </React.Fragment>
  );
}

export default SignUp;