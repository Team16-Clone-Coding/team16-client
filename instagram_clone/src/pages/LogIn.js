import React from "react";
import { history } from "../redux/configStore";

const LogIn = (props) => {
  return(
    <React.Fragment>
      로그인페이지~~~~~~~~~~!!!
    </React.Fragment>
  );
}

export default LogIn;